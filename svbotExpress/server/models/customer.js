var mongoose = require('mongoose');

// Define our beer schema
/*
var customerSchema = new mongoose.Schema({
    Name: String,
    Mobile: String,
    Phone: String,
    Addresses: [](Flat, Street, State, Pin Code) ,
    Email: String,
});

// Export the Mongoose model
*/